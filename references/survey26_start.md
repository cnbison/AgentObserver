# 新手上路 · 从零到第一个成绩

> 巡天智能体 · Agent Observer · 参赛指引
>
> 来源页面：<https://bh3gei.github.io/agent-observer/start>
>
> 不需要懂命令行，也不用装任何第三方库。跟着走一遍，**大约 20 分钟**就能把第一个成绩挂上排行榜。
>
> 整件事就三件：**跑起来 → 改一个函数 → 上传**。
>
> [先去报名 ↗](https://bh3gei.github.io/agent-observer/register) · [下载入门包 ↓](https://bh3gei.github.io/agent-observer/resources) · [完整文档 →](https://bh3gei.github.io/agent-observer/docs) · [返回首页 ↗](survey26.md)

---

## 目录

1. [注册账号](#第-1-步--注册账号)
2. [建一个队伍](#第-2-步--建一个队伍)
3. [下载入门包，先跑通](#第-3-步--下载入门包先跑通)
4. [只改一个函数](#第-4-步--只改一个函数)
5. [上传](#第-5-步--上传)
6. [看结果](#第-6-步--看结果)
7. [卡住了？](#卡住了)

---

## 第 1 步 · 注册账号

点页面右上角的「报名」，填姓名、邮箱、密码，勾上 *"我已阅读规则"*，点 **创建账号 →**。

- **不用去邮箱点确认链接**，提交完就直接登录进「控制台」了。
- GitHub 用户名和所属机构可以留空，以后在「个人资料」里补。

---

## 第 2 步 · 建一个队伍

控制台会提示 *"你还没有队伍"*。点「队伍 →」，填个队名，点 **创建队伍 →**。

> ⚠️ **一个人参赛也要建队伍**——成绩是按队伍记的。

- 建完会给你一个邀请码（形如 `E3SE6NE9`），队友在「队伍」页填这个码就能加入。
- 每支队伍**最多 8 人**。

---

## 第 3 步 · 下载入门包，先跑通

控制台右侧有「下载入门包 ↓」（「资源」页也有）。下下来解压，得到一个 `agent-observer-starter-kit` 文件夹。

### 双击对应系统的运行脚本

| 你的电脑 | 双击这个 |
|---|---|
| **macOS** | `run_baseline.command`（系统自带的 Python 就够用；如果提示"无法打开"，右键 →「打开」） |
| **Windows** | `run_baseline.bat`（先去 [python.org](https://www.python.org/downloads/) 装 Python 3.12，**安装时记得勾上 Add python.exe to PATH**） |
| **Linux** | 终端里运行 `./run_baseline.sh` |

### 预期结果

约 **15 秒后**会自动弹出一个网页，那是基线智能体在 **180 个观测夜**上的回放。终端最后一段是分数：**基线大约 12287 分**，`termination_reason` 显示 `survey_complete` 就说明一切正常。

> 💡 **想先快速看一眼？** 把文件名里的 `run_baseline` 换成 `run_demo_week`，是 7 个观测夜的演示场景，2 秒跑完，回放也更容易一夜一夜看清楚。

---

## 第 4 步 · 只改一个函数

打开 `agent/my_strategy.py`。**整个比赛你只需要改这个文件里的 `choose_action` 函数。**

它的工作很简单：

- 平台把 *"这一时隙能观测的候选"* 排好序交给你，**第 0 个是系统估计收益最高的**；
- 你挑一个返回，或者返回 `None` 表示这一时隙先等着。

文件里已经写好了几条思路，**取消注释就能用**：

- 优先补必做天区（REQUIRED）
- 优先响应观测请求
- 条件差的时候等待
- 用 `memory` 记住已经做过什么

每个候选带哪些字段、各是什么意思，都写在文件开头。

> 🔧 改完保存，再双击一次 `run_baseline`，看分数有没有涨。跑出错的话，终端会直接把 `agent.log` 的最后几行打出来。

---

## 第 5 步 · 上传

回到网站，点「提交」。**有两条路，按现在是哪个阶段选**：

### 练习赛 · 传结果文件

选「结果文件」，把刚才本地跑出来的 `decisions.csv` 拖进框里，点 **上传并排队评测 →**。几秒钟出分。

> 练习赛用的是天气公开的场景，所以你在本地算出来的结果平台可以直接复核。

### 正式比赛 · 传智能体

选「智能体运行」，把 `agent/my_strategy.py` **这一个文件**拖进去就行——网站会自动把入门包里其余的标准文件补齐、打好包，你不用自己压缩。

> 如果想换模型或用大模型，把整个 `agent` 文件夹拖进去。

平台会在**隐藏天气**的场景上运行你的程序，你看不到未来天气，**这才是真正比的东西**。

---

## 第 6 步 · 看结果

上传后页面会显示排队位置和评测进度，通常一两分钟出分。点进这条提交，能看到：

- **分数分解** —— 基础科学分、项目加成、请求奖励，以及各项扣分分别是多少
- **逐夜回放** —— 每个观测夜实际做了什么，哪些天区亮了、哪些没排上
- **运行日志** —— 程序在平台上的真实输出

排行榜在顶部导航「排行榜」里，实时更新。

---

## 卡住了？

| 情况 | 怎么办 |
|---|---|
| 双击没反应 / 提示找不到 Python | Windows 要先装 Python 3.12 并勾选 *Add to PATH*；macOS 右键 →「打开」 |
| 上传后一直排队 | 正常，评测机在逐个跑；场景大的时候要等一会儿 |
| 提交立刻变成 `invalid` | 多半是打包方式不对。直接拖 `agent/my_strategy.py` 单个文件最省事 |
| 想在本地多试几种天气 | `python3 make_scenario.py --out scenarios/mine --seed 7 --days 30`，再 `python3 local_runner.py --scenario scenarios/mine --agent agent/minimal_agent.py` |
| 想让大模型参与决策 | 复制 `agent/.env.example` 为 `agent/.env`，填好 key（「控制台」页可以领赞助额度），上传时拖整个 `agent` 文件夹 |

更细的数据格式、协议和评分公式在「文档」页；入门包里的 `README.md` 是给工程师看的完整版。

有其他问题看「常见问题」页，或者在「公告」页找主办方联系方式。

---

**900 S · 每 15 分钟一次决策 · 全程共用一个总时长** 时隙 `N20260908-S019` · `N20260908 · 圆顶开启`

---

**OPEN / OBSERVER** · 2026 GOSIM · 巡天智能体

[规则 ↗](https://bh3gei.github.io/agent-observer/rules) · [文档 ↗](https://bh3gei.github.io/agent-observer/docs) · [公告 ↗](https://bh3gei.github.io/agent-observer/announcements) · [联系](mailto:hackathon@gosim.org) · [GOSIM ↗](https://gosim.org)